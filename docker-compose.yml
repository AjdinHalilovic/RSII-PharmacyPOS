version: '3.4'

networks:
  dockerapi-dev:
    driver: bridge 

services:
  dockerapi:
    image: dockerapi:latest
    depends_on:
      - "postgres_image"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 58643:5050     
    environment:
      DB_CONNECTION_STRING: "host=postgres_image;port=5432;database=160048;username=ajdinuser;password=ajdinuser"
    networks:
      - dockerapi-dev  
  
  postgres_image:
    image: postgres:latest
    ports:
      - "5432"
    restart: always
    volumes:
      - db_volume:/var/lib/postgresql/data
      - ./dbscripts/seed.sql :/docker-entrypoint-initdb.d/seed.sql
    environment:
      POSTGRES_USER: "ajdinuser"
      POSTGRES_PASSWORD: "ajdinuser"
      POSTGRES_DB: "160048"
    networks:
      - dockerapi-dev
volumes:
  db_volume:
         
    
     